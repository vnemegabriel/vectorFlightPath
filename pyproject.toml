[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "vectorFlightPath"
version = "0.1.0"
description = "2D agent-based mosquito host-seeking model for spatial isoprotection surfaces"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24",
    "scipy>=1.10",
    "tomli>=2.0; python_version < '3.11'",
]

[project.optional-dependencies]
dev = ["pytest>=7.4", "matplotlib>=3.7"]

[project.scripts]
vfp = "vfp.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
vfp = ["presets/*.toml"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "slow: studies that take minutes (convergence, invariance, foppa factorial)",
]
